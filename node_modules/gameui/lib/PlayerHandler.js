'use strict';

var PlayerHandler = function(el, game) {
  this.el = el;
  this.game = game;

  this._onPlayerChange = this._handlePlayerChange.bind(this);
  this.game.on('player', this._onPlayerChange);
};

PlayerHandler.prototype.destroy = function() {
  this.game.off('move', this._onMove);
};

PlayerHandler.prototype._handlePlayerChange = function(playerId , lastPlayerId) {
  // TODO
  var el = document.createElement('div');
  el.innerHTML = 'player change from '+lastPlayerId+' to '+playerId;

  window.requestAnimationFrame(function() {
    this.el.appendChild(el);
  }.bind(this));
};

module.exports = PlayerHandler;
